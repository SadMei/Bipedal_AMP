# BSRL AMP Debug List

按“改动 + 效果”记录关键训练版本，便于后续回溯。

## 关键版本

- **第一版成功训练（2026-07-22）**：使用旧 URDF、原生 Implicit PD、默认膝角 `-0.6`、指令范围 `x/y=±1.0`、yaw `±0.4`，`joint_pos_penalty=-1.0`。**效果**：可以稳定行走，作为基准版本。
- **新坐标模型（2026-07-30）**：统一新的 URDF/USD 关节轴、限位和 GMR/AMP 关节符号。**效果**：模型和参考动作坐标一致，但策略表现明显变保守。
- **贴地默认姿态（2026-08-02）**：采用 GMR 第一帧整理得到的默认根高 `0.8665`、膝角 `+0.4`。**效果**：初始站姿和脚底接触正常。
- **第一版动力学与事件复原**：恢复第一版 PD、armature、限位及随机事件，保留 delayed actuator 框架但将延迟设为 `0`。**效果**：排除了动力学和事件差异导致训练失败的影响。
- **`joint_pos_penalty=-1.0`（2026-08-02）**：只使用新 URDF 和贴地默认姿态训练。**效果**：可以稳定移动，但关节幅度偏小，末段风格奖励约 `0.069`，腾空奖励约 `0.076`。
- **`joint_pos_penalty=-0.3`（2026-08-02）**：仅降低关节默认姿态偏离惩罚，其余配置不变。**效果**：动作幅度明显增大，末段风格奖励约 `0.080`，腾空奖励约 `0.093`，平均 episode 长度约 `979/1000`，mixed 播放稳定。

## 当前实验

- **`0~4` 物理步随机延迟 + 高速指令**：保持 `joint_pos_penalty=-0.3`、新 URDF、默认姿态、PD 和其他事件不变；执行器每次 reset 为各环境随机采样 `0~4` 步延迟，速度范围扩展为 `lin_vel_x=±2.0`、`lin_vel_y=±1.2`、`ang_vel_z=±0.6`。**效果**：完成 10000 轮，平均 episode 长度约 `976/1000`，最终 XY 速度误差约 `0.71`；高速指令可执行，但对较小前向指令的响应偏弱。
- **机身质心 + PD 增益随机化（2026-08-04）**：在上一版 `0~4` 步延迟和高速指令配置上，增加 `base_link` 质心偏移（x `±0.025 m`、y/z `±0.05 m`）以及全关节 Kp/Kd `0.8~1.2` 倍缩放，其余配置不变。**效果**：平均 episode 长度约 `953/1000`，但最终 XY 速度误差恶化到约 `2.26 m/s`，线速度跟踪奖励由上一轮的约 `2.12` 降到 `0.29`；从静止默认姿态播放时 6 秒净位移只有约 `5 mm`，策略退化为以站立和转向为主。
- **去除全关节默认姿态惩罚（2026-08-04）**：以上一版延迟、高速指令、COM 和 PD 增益随机化配置为基线，只将 `joint_pos_penalty=-0.3` 改为 `0.0`；保留 hip yaw/roll 偏离惩罚、关节限位、AMP 风格和动作平滑约束。**效果**：最终 XY 速度误差约 `0.84 m/s`、线速度跟踪奖励约 `1.91`、腾空奖励约 `0.114`，相较上一版 `2.26 m/s` 和 `0.29` 明显恢复；mixed 播放中 `vx=0.6 m/s` 时基本站立，切换到 `vx=1.0 m/s` 后才启动，但启动后的步幅较大。**结论**：去除姿态惩罚有效释放了动作幅度并恢复速度跟踪，但策略仍存在低速不响应的指令死区，启动阈值位于 `0.6~1.0 m/s` 之间。
- **完整域随机化 + 收紧速度奖励（2026-08-04）**：保持高速指令、`joint_pos_penalty=0` 和 `0~4` 步延迟；启用全身摩擦 `0.5~1.5`、恢复系数 `0~0.2`、关节零位 `±0.05 rad`、现有 COM 与 Kp/Kd 随机化、全身质量及惯量 `×0.8~1.2`、armature `×0.8~1.2`、关节摩擦 `0~0.05`，并将线速度奖励 `std` 从 `0.5` 降至 `0.3`。**效果**：完成 `8192 env / 30000轮`；末 500 轮平均 episode 长度约 `961/1000`、风格奖励约 `0.231`，但 XY 速度误差约 `2.36 m/s`、线速度跟踪奖励仅约 `0.121`，相比上一版约 `0.80 m/s` 和 `1.90` 明显退化。mixed 播放中机器人主要维持站立，只做轻微重心和转向调整，未形成有效位移。**结论**：训练已收敛到偏重存活和 AMP 风格的局部最优，继续增加轮次没有改善速度跟踪；该 checkpoint 不宜作为下一轮训练基线。
- **速度范围/跟踪精度课程 + 大误差惩罚（2026-08-05，训练中）**：线速度跟踪奖励权重由 `3.0` 提高到 `5.0`；在 `0~5k / 5k~12k / 12k~20k` 三阶段中，将 `vx` 从 `±1.0` 扩到 `±1.5/±2.0 m/s`、`vy` 从 `±0.5` 扩到 `±0.8/±1.2 m/s`、yaw 从 `±0.3` 扩到 `±0.45/±0.6 rad/s`，同时将线速度奖励 `std` 从 `0.5` 收紧到 `0.4/0.3`。新增阈值 `0.25 m/s`、权重 `-1.0` 的超额线速度误差平方惩罚，并将超额误差截断在 `2.0 m/s`；保持完整域随机化、`0~4` 步延迟和 `joint_pos_penalty=0`。**训练设置**：`8192 env / 20000轮`，每 `1000` 轮保存一次 checkpoint；run 后缀为 `veltrack5_curriculum_full_dr_delay04_8192_20k`，最终效果待补充。
- **速度课程中途 checkpoint（2026-08-06）**：`model_11000.pt` 的 XY 速度误差约 `0.58 m/s`、线速度跟踪奖励约 `3.36`，但风格奖励约 `0.036`，髋 yaw/roll 偏离惩罚约 `-0.206`；播放中出现明显八字腿，说明提高 task 速度奖励后，策略用增大支撑宽度换取速度和鲁棒性，AMP 风格约束不足。训练在 `11988` 轮发生数值发散：action-rate 惩罚约 `-1.4e9`，策略噪声标准差变为非法值，未产生 `model_12000.pt`。**结论**：后续应恢复温和的全身姿态约束（此前 `joint_pos_penalty=-0.3` 的幅度和风格更平衡），并限制策略噪声/增加训练数值稳定措施。
- **速度奖励权重单变量回退（2026-08-06，训练中）**：以上一轮速度课程、超额速度误差惩罚、完整域随机化、`0~4` 步延迟和 `joint_pos_penalty=0` 为基线，仅将 `track_lin_vel_xy_exp` 权重从 `5.0` 降回 `3.0`。**训练设置**：`8192 env / 20000轮`，每 `1000` 轮保存一次 checkpoint；run 后缀为 `veltrack3_curriculum_full_dr_delay04_8192_20k`，用于对比速度跟踪和 AMP 风格是否重新平衡，最终效果待补充。

## 调参经验

- **训练轮次要结合速度跟踪曲线判断**：正常的 `joint_pos_penalty=-0.3` 版在 8000 轮前后已收敛，XY 速度误差约 `0.41 m/s`；延迟加高速指令版在 2000/5000/8000/9999 轮约为 `2.18/1.23/0.80/0.71 m/s`，末期仍在改善，延长训练可能有效。
- **总奖励上涨不代表学会移动**：COM 与 PD 增益随机化版的 XY 速度误差在 2000～9999 轮一直约为 `2.1~2.3 m/s`，但总奖励和 episode 长度仍上涨。**结论**：策略主要学会了存活和站立，直接增加训练轮次可能继续巩固局部最优，应重点观察速度跟踪项和实际播放效果。
- **不要一次加入多个高影响改动**：`0~4` 步延迟和高速指令同时加入后首次出现起步退化，当前结果无法区分两者贡献。后续应分别只加延迟、只扩大指令范围进行消融。
- **高速能力与低速响应应分开评估**：取消默认姿态惩罚后，`vx=1.0 m/s` 可触发大步幅运动，但 `vx=0.6 m/s` 仍选择站立。只看高速视频会高估整段速度指令的可控性，后续应单独测试多个固定前向速度并关注启动阈值。
- **速度跟踪核过宽会容忍低速静止**：当前线速度跟踪奖励为 `3.0 * exp(-error^2 / 0.5^2)`。机器人静止且指令为 `0.6 m/s` 时，该项仍约为 `0.71`；指令为 `1.0 m/s` 时则只有约 `0.055`。**结论**：当前 `std=0.5` 很可能促成了 `0.6 m/s` 站立、`1.0 m/s` 才启动的阈值行为；若后续要求低速也严格跟踪，应优先减小 `std` 或调整低速指令采样，而不是继续增加训练轮次。
- **慢走数据没有与低速指令建立条件对应**：`walk1_subject1.pkl` 的平面速度中位数约 `0.515 m/s`，约 `40.2%` 帧位于 `0.3~0.6 m/s`、`16.4%` 位于 `0.6~0.8 m/s`，慢走样本本身充足。但 AMP 判别器既不观察速度指令，也不观察根线速度，参考 walk/run 又以无条件混合分布采样；actor 也没有直接的根线速度观测，只有 critic 使用该特权信息。当前总奖励按 `40% task + 60% style` 混合。**结论**：AMP 只能要求策略像数据集中的任意动作，无法自行学习“`0.6 m/s` 指令应选择慢走”；低速映射主要依赖当前偏宽的任务速度奖励，因此产生站立死区。

## 已确认问题

- **静止默认姿态不是起步退化的根因（2026-08-04）**：`grounded_delayed_first` 与 `joint_pos_penalty=-0.3` 两版策略均能从默认关节姿态、零根速度立即起步；其中前者在强前向播放指令下 3 秒前进约 `2.09 m`。**结论**：此前“训练未见静止默认姿态导致无法起步”的判断被反例推翻。
- **退化首次出现在延迟与高速指令同时加入后（2026-08-04）**：相对正常的 `joint_pos_penalty=-0.3` 版本，`delay0to4_fast_commands` 只同时增加了 `0~4` 物理步延迟，并将指令范围扩大到 `lin_vel_x=±2.0`、`lin_vel_y=±1.2`、`ang_vel_z=±0.6`。**效果**：从默认姿态播放时前 12 秒几乎不前进；需要分别只加延迟、只扩大指令各训练一轮，才能确定主因。
- **mixed 播放指令时序（2026-08-04，已修复）**：最初加入 mixed profile 的 `play.py` 在取得首帧观测后才写入自定义指令，因此策略看到的指令比脚本设置晚一个控制步（约 `20 ms`）；继承的 `10 s` CommandManager 重采样还会在该时刻短暂插入一帧 Play CFG 的固定前向指令。现已在创建环境前配置首段 mixed 指令并关闭周期重采样，后续分段切换也在下一帧观测生成前写入。标准 `BSRL-Play-v0` 原本就已关闭 heading 和 standing 覆盖，摄像机、视频生成和 `config` profile 均未改动。**结论**：该问题只影响 mixed 指令的严格时序，不能解释机器人连续数秒后退。
